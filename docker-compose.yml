version: "3"

services:
  backend:
    build: ./backend/
    ports:
      - 8000:8000
    volumes:
      - ./backend:/app
    command: bash -c 'uvicorn sample:app --reload --host 0.0.0.0 --port 8000'
